# v1.7.4

**Tag:** `v1.7.4`
**Date:** 2026-08-10（规划）
**Previous:** `v1.7.3`

---

## Overview

v1.7.4 是 **1.0 稳定化路线的工程化收口版本**。相比 v1.7.3 的架构重构，
本版本聚焦四件事：**冻结 API 面、建立发布纪律、补齐插件与示例体系、
落地生产验证工具**，为 1.0 发布扫清工程化障碍。

核心主题：从"功能驱动的快速演进"切换到"稳定驱动的 1.0 收口"。

## Breaking Changes

### 移除 `crawlo.bot` 兼容存根

`crawlo.bot`（含 channels/core/monitoring/templates/utils 子包）已在 1.7.4
**提前移除**（原计划 v2.0，项目决策提前），属刻意 breaking change：

```python
# ❌ 1.7.4 起抛 ModuleNotFoundError
from crawlo.bot import get_notifier, ChannelType

# ✅ 唯一合法路径
from crawlo.extensions.notifications import get_notifier, ChannelType
```

> 迁移示例见 [通知指南](../guides/notification-guide.md)。

### Python 版本要求提升至 3.10+

`python_requires` 从 `>=3.8` 提升到 `>=3.10`（代码已使用 3.10+ 类型语法，
CI 矩阵实际覆盖 3.10–3.14）。

## New Features

### 1. API 面冻结（1.0 前置）

- 新增 [api-surface.md](../reference/api-surface.md)：**454 个公共符号**
  100% 覆盖审计，逐项标注 frozen / experimental / optional / internal；
- 兼容性守护扩展：签名守护从 5 个核心类扩展到 **57 类 / 471 个方法**；
- 新增 import-path 兼容矩阵测试（97 个模块路径 + 34 组顶层符号 + shim 迁移等价）；
- **效果**：任何"删符号不改测试"的 PR 会被 CI 直接拦截。

### 2. 发布纪律

- 新增 `crawlo release --dry-run` 发布就绪检查：
  semver 版本号 / CHANGELOG 条目 / 发布说明 / git tag 一致性；
- 新建 `CHANGELOG.md`（Keep a Changelog 格式）+ 发布模板
  `docs/releases/_TEMPLATE.md`；
- CI 新增 release-check 门禁（PR 合入 main + 版本 tag 推送时强制检查）。

### 3. 插件机制（P1）

- 新增 `crawlo.plugin` 统一注册表：
  `register_middleware` / `register_pipeline` / `register_extension`
  （含对应 `unregister_*`）；
- **双通道配置**：注册表短名称 + 完整字符串路径
  （`load_object` 统一解析，路径优先）；
- 官方示例 `examples/plugin_hello_world/` + 开发指南
  [plugin-development.md](../guides/plugin-development.md)。

### 4. 生产示例体系（P2/P3）

- `examples/real_world_catalog/`：整站抓取 cookbook
  （分页 → 详情 → 去重 → JSONL/MySQL 存储 → 监控 → 分布式），
  含本地 mock 站与 CI 冒烟测试；
- `examples/simple_quickstart/`：**23 行**跑通第一个爬虫
  （与全部示例同网站 ee.ofweek.com）；
- `docs/guides/when-to-use.md`：场景决策树（什么时候用什么配置）。

### 5. 稳定性与生产验证工具（P4）

- `scripts/stress_run.py`：长跑压测（RSS / 对象泄漏斜率 / 事件循环延迟）；
- `scripts/benchmark/`：Crawlo vs Scrapy 性能基准；
- `scripts/redis_ha/`：Sentinel 故障切换演练（已端到端实测）；
- `scripts/failure_inject.py`：故障注入（redis-down / network-partition 等）；
- 部署文档 5 篇：Docker / Kubernetes / 监控告警 / 代理轮换 / 升级回滚。

## Improvements

- **Deprecation 治理**：`filterwarnings = error::DeprecationWarning` 全局强制，
  框架内部 40+ 处旧路径引用迁移到新路径；
- **文档体系重构**：全站审计（修复 13 处失效导入、20+ 断链、过时版本声明），
  大文档拆分（分布式架构 1293→385 行），部署文档从 1 篇扩到 6 篇；
- **调度日志**：`Filtered duplicate request` 降为 debug 级别，关闭时
  打印汇总条数，消除刷屏。

## Bug Fixes

- `HttpXDownloader` 改用 Cookie header 合并，消除 httpx 0.28+ per-request
  cookies 废弃警告（该警告在 `-W error` 下会中断爬取）；
- `reset_global_context()` 同步重置 CoreInitializer 单例：
  修复 settings 缓存跨测试/跨项目泄漏；
- `CoreInitializer.initialize()` 增加防御：initializer 注册表被清空时
  自动注册内置初始化器；
- 修复 redis-py 5.x 废弃 `close()` → `aclose()` 迁移
  （stream / priority / filter / pool / cluster / pipeline 共 6 处）；
- `BackpressureableQueueMixin.__init__` 正确初始化 `_stats/_name/_max_size`
  （修复 DiskQueue 实例化报错）；
- Redis Stream 死信升级时不再丢失已投递消息；补回 `_SEED_LOCK_LUA`；
- `genspider` 模板修复：item_class 解析误把导入的 `Item` 算作项目类、
  模板 yield 字典 → 生成的爬虫可直接运行。

## Migration Guide

### 1. 通知系统（必须迁移）

所有 `crawlo.bot` 导入迁移到 `crawlo.extensions.notifications.*`：

```python
from crawlo.extensions.notifications import get_notifier, ChannelType
```

### 2. Python 环境

确保运行环境 Python ≥ 3.10（3.10–3.14 已适配）。

### 3. 无其他破坏性变更

1.7.4 除上述两项外**无 API 破坏**——其余新增均为向后兼容。

## Known Limitations（已知限制）

以下为分布式设计的已知取舍，1.7.4 保留现状，1.0 后视需求评估改进：

### 1. 单 Consumer Group 模型（无分区/顺序保证）

所有 Worker 竞争同一消费组，任务**无分区、无顺序保证**。爬虫场景够用
（任务间无强依赖），但同一域名的请求顺序无法保证——对站点友好性有
影响，由 `DOWNLOAD_DELAY` 兜底。

### 2. 可观测性缺口：pending/lag 未暴露为 Prometheus 指标

`pending_info()` 能查询 pending 数，但**未暴露为指标**。生产监控缺少
"消费积压（lag）"这一关键信号。建议后续把 pending/lag 加入
[监控告警文档](../deployment/monitoring-alerting.md) 的指标清单。

### 3. 投递语义为 at-least-once（需去重兜底）

XACK 前崩溃 → XCLAIM 重投 → 下游可能重复写入。当前由
`RedisDedupPipeline` 兜底，但业务侧需理解 **"at-least-once + 去重"**
的语义，幂等性不在框架层保证。完整说明（重复场景 + 幂等保障建议）
见 [分布式投递语义](../concepts/distributed-reliability.md#投递语义at-least-once至少一次)。

### 4. 死信无自动重放

DLQ 只能手动 `crawlo dead-letter retry`，无定时重放策略。偶发失败场景
够用；高频失败场景需外部编排（如定时任务扫描死信重试）。

### 5. 优先级仅双档

高/普通两个 Stream（`priority < 0` 走高优），无多级优先级队列。
爬虫场景可接受。

## Verification

- 全量测试 **2753 项收集**，CI 全绿（测试 / lint / release-check 三 job）；
- API 覆盖审计 100%（454/454 符号）；
- 10 Worker 真实站点分布式测试通过（种子锁 / Stream 分发 / 协调退出）；
- Redis Sentinel 故障切换端到端实测通过（master 切换 15.4s，爬虫无中断）。

---

> 文档配套：`docs/releases/v1.7.3.md` 已同步"后续演进"章节，
> 本版本变更详录于 [CHANGELOG.md](../../CHANGELOG.md) Unreleased。
